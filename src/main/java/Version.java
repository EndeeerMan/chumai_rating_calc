/**
 * The B50 pool to which a chart belongs.
 */
public enum Version {
    /** Older songs; the highest 35 contributions are selected. */
    LEGACY,

    /** Songs from the current version; the highest 15 are selected. */
    CURRENT
}
